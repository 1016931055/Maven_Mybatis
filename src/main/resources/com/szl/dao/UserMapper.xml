<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 利用namespace属性，绑定当前映射文件和Mapper接口映射关联 -->
<mapper namespace = "com.szl.dao.UserMapper">

    <!-- SQL查询语句 -->
    <!-- id是方法名 -->
    <select id="findAllUser"
            resultType="com.szl.pojo.User">
        select id, username, birthday, sex, address
        from user
    </select>

    <!-- 根据id查询用户数据-->
    <!-- mybatis 可自动推导参数,故可省略 parameterType这一项-->
    <select id="findUserById" parameterType = "_int"
            resultType="com.szl.pojo.User">  <!-- 查询的数据封装到User中 -->
        select id, username, birthday, sex, address
        from user
        where id = #{id}
    </select>

    <!-- 插入数据-->
    <insert id="addUser">
        insert into user(username, birthday, sex, address)
        value(#{username}, #{birthday}, #{sex}, #{address})
    </insert>

    <update id="updateUser">
        update user set username = #{username}, sex = #{sex}, address = #{address}
    </update>

</mapper>